# 001 · sheRuntime iOS 基线导入

- 日期：2026-09-01
- 分支：`feat/refector`
- 状态：代码导入、模拟器编译和 iPhone 真机验证均已完成

## 本批目标

把原 sheRuntime iOS 工程复制到 robo，建立后续 Apple Watch Target 合并所依赖的 iPhone 基线。本批不修改业务逻辑、视觉、算法、Target、Scheme、Bundle ID、Deployment Target 或 Swift 类型名。

## 来源与目标

| 来源 | robo 目标 | 操作 |
|---|---|---|
| `/Users/ari/Desktop/she-runtime/apps/ios/sheRuntime/` | `Apple/sheRuntime/` | 复制，不移动 |

原始目录 `/Users/ari/Desktop/she-runtime` 和 `/Users/ari/Desktop/ENERGY-Watch` 均未修改。

## 已复制内容

- `sheRuntime.xcodeproj`
- `sheRuntime` App 源码和资源
- `sheRuntimeTests`
- `sheRuntimeUITests`
- shared scheme `sheRuntime`
- HealthKit entitlement

## 有意排除

- `ask_secrets.local.json`：本地开发者配置，不进入 robo，不允许提交或随 App bundle 分发。
- `xcuserdata/`、`*.xcuserstate`：Xcode 用户私有状态。
- `.DS_Store`：macOS 文件系统元数据。

除上述排除项外，使用 `rsync -ani --delete` 对比来源与目标，没有发现差异。

## 当前工程身份

| 项目 | 当前值 |
|---|---|
| Project | `sheRuntime.xcodeproj` |
| App Target | `sheRuntime` |
| Scheme | `sheRuntime` |
| Bundle ID | `com.arieslx.sheRuntime` |
| Deployment Target | iOS 26.5 |
| Entitlement | HealthKit |

这些名称将在后续独立批次中讨论和修改，本批保持不变。

## 自动验证

执行：

```bash
cd Apple/sheRuntime
xcodebuild -project sheRuntime.xcodeproj -scheme sheRuntime \
  -configuration Debug \
  -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/robo-batch-001-ios \
  build CODE_SIGNING_ALLOWED=NO
```

结果：`BUILD SUCCEEDED`。

## 真机验证清单

请从 robo 中打开 `Apple/sheRuntime/sheRuntime.xcodeproj`，选择 `sheRuntime` Scheme 和 iPhone 真机后运行，并检查：

1. App 能安装和启动，五个 Tab 均能打开。
2. HealthKit 权限页可正常出现，授权状态可读取。
3. iPhone 录音、停止、转写、确认和时间线保存流程正常。
4. M5Stack StopWatch 的蓝牙入口仍可打开；有设备时检查连接和录音接收。
5. Ask 页面能打开。由于本地 secret 未复制，请只确认无凭据时的现有降级或错误表现，不要把密钥写入仓库。

## 真机结果

- 结果：用户于 2026-09-01 确认测试完成，可以进入下一批。
- 设备型号、iOS 版本和 Signing Team：本批未记录。
- 已知失败项：无用户报告。

## 下一批门槛

本批门槛已通过。下一批不会直接迁入 Watch 业务代码，先建立可嵌入、可独立编译的空 Watch Target 和 Scheme。
