# 003 · 剩余迁移工作量与批次拆分

- 日期：2026-09-02
- 分支：`feat/refector`
- 状态：003A–003F 与文件级命名清理均已完成并通过测试；003G 待开始

## 当前完成度

截至 2026-09-03，Apple 双端功能迁移主体已经完成：

- Robo iPhone Target 与独立 `Robo Watch App` Target 已由同一 Xcode 工程管理，Target Dependency、Watch 嵌入、Bundle ID 和 companion ID 已建立并验证。
- ENERGY Watch 的视觉、录音运行时和 WatchConnectivity 文件传输已迁入 Robo；原 ENERGY-Watch 与 she-runtime 仓库未修改。
- Shared 已建立小型 Models/Contracts，包含 `EnergyTier`、iPhone/Apple Watch 两种记录来源、版本化消息 envelope 和去重器；Robo 不再包含 M5Stack/CoreBluetooth 功能。
- Watch 在线与离线录音均可传到 iPhone；iPhone 未打开时收到的文件及 sidecar 可持久恢复，并按 FIFO 逐条进入现有端侧转写、文本确认和 Timeline 保存流程。
- iPhone、Apple Watch 真机回归以及 `Product → Test` 已由用户完成并通过。
- 测试 Target、Bundle ID、目录和文件展示名已迁为 `RoboTests` / `RoboUITests`；iPhone 源码同步目录已迁为 `iPhoneApp`，签名路径已修复并通过双端验证。

当前功能迁移与命名清理约完成 95%。剩余工作是 003G 最终回归记录、最终目录/迁移表/遗留问题和 Git 整理。

## 当前准确断点

已验证且不可重复执行的最后一步：

- `sheRuntime` 同步目录已通过 Xcode 改为 `iPhoneApp`。
- Robo Target Debug/Release 的 `CODE_SIGN_ENTITLEMENTS` 已改为 `iPhoneApp/sheRuntime.entitlements`。
- 用户随后完成 Robo iPhone 真机启动、原页面检查、`Product → Test` 和 `Robo Watch` 真机启动，全部通过。

当前已完成并验证：

1. `iPhoneApp/RoboApp.swift`、`RoboApp` 入口类型与 `iPhoneApp/Robo.entitlements` 已完成重命名，Debug/Release 签名路径已更新。
2. Robo entitlements 已与原始基线对齐，仅保留 HealthKit。
3. UI Test 类名、用户可见的 `sheRuntime` 提示、logger fallback 和源码文件头已完成产品名清理；Robo 源码扫描不再出现 `sheRuntime`。
4. 用户已完成 Product Test、iPhone 真机和 Apple Watch 真机回归，全部通过。文件级命名批次关闭，下一步为 003G。

### 文件级命名清理执行状态

- 2026-09-03：用户确认开始执行。第一步仅通过 Xcode 将 App 入口文件 `sheRuntimeApp.swift` 改为 `RoboApp.swift`；本步不同时修改 Swift 类型、entitlements 或其他旧文案，完成后先做路径核对。
- 用户已通过 Xcode 完成文件重命名；只读核对确认 `iPhoneApp/RoboApp.swift` 存在、旧文件不存在，文件同步根仍为 `iPhoneApp`。
- 已将该文件的 `@main` 类型由 `sheRuntimeApp` 改为 `RoboApp`，并仅同步更新文件头名称；App Scene、ModelContainer、依赖注入和生命周期逻辑均未修改。等待 iPhone 编译/启动验证后再处理 entitlements 文件名。
- 用户已在 iPhone 真机完成 `Robo` Build/Run，确认 App 正常启动并进入原页面；App 入口文件与类型重命名验收通过。
- 下一步仅通过 Xcode 将 `iPhoneApp/sheRuntime.entitlements` 改为 `iPhoneApp/Robo.entitlements`，随后单独核对并修正 Debug/Release `CODE_SIGN_ENTITLEMENTS`。
- 用户已通过 Xcode 完成 entitlements 文件重命名；`iPhoneApp/Robo.entitlements` 存在，旧文件已不存在。
- Xcode 未自动更新构建设置：Robo Target Debug/Release 仍指向 `iPhoneApp/sheRuntime.entitlements`。在改为 `iPhoneApp/Robo.entitlements` 前暂停编译，避免将签名路径错误误判为源码问题。
- 用户已在 Xcode 将 Robo Target Debug/Release 的 `CODE_SIGN_ENTITLEMENTS` 改为 `iPhoneApp/Robo.entitlements`；只读核对确认路径和 plist 有效。
- 内容核对发现重命名后的文件多出空值 `com.apple.developer.applesignin`，而原始 she-runtime entitlements 仅包含 HealthKit。经用户确认，已移除该非原始能力，只保留 `com.apple.developer.healthkit = true`；未修改工程文件或其他 Capability。
- 用户完成 iPhone 真机签名、HealthKit 页面与 `Product → Test` 验证，entitlements 文件重命名批次正式完成。
- 文件级源码清理最后一批将 UI Test 类名改为 `RoboUITests` / `RoboUITestsLaunchTests`，将残留用户提示、旧 Debug ContentView、logger fallback 与文件头中的产品名统一为 Robo；不修改业务逻辑或本地化基础设施。
- 用户完成本批最终验证：`Product → Test`、Robo iPhone 真机页面/录音/HealthKit、Robo Watch 启动/录音均通过；文件级命名清理正式完成。

### DEBUG-only Watch 传输探针说明

- iPhone “我的 → 数据与隐私”页面右上角的爬虫图标实际使用 SF Symbol `ladybug`，仅在 Debug 构建出现；入口打开 `WatchTransferProbeView`，用于查看 WCSession 激活/可达状态、接收时间、录音时长、文件大小、messageId，并在文件仍存在时试听。
- 该探针与正式业务流程隔离，Release 不包含入口，可随时删除或扩展，不是用户正式功能。
- 003F 接入自动业务流程后，Watch 录音到达即进入转写与确认队列；用户保存或点击“取消并删除”时，`VoiceCaptureViewModel` 会删除 m4a 和 JSON sidecar。Connectivity service 的内存列表仍保留该条接收记录，因此探针可能继续显示条目，但“播放”无法打开已被业务流程消费的文件。
- 这是当前文件生命周期设计导致的已知调试限制，不代表 Watch 传输或转写失败。003G 决定是让探针显示“已处理/文件已删除”并禁用播放，还是在最终迁移验证后直接移除探针；在用户确认前不延长正式音频留存时间。

## 初始源码规模（历史基线）

- Robo 当前 iPhone 目录约 76 个文件。
- ENERGY Watch 待评估 Swift 代码共 8 个文件、约 908 行。
- 其中 Watch UI/运行时约 515 行，iPhone Demo/Connectivity 约 305 行，共享 `EnergyTier` 约 88 行。
- ENERGY Watch 当前协议只使用 `tier` 等简单字典字段，尚无 `protocolVersion`、`messageId`、`sessionId`、`eventType`、`createdAt` 或去重机制。

## 建议批次

以下为迁移启动时的批次定义；其中 003C 的 M5Stack 设想已被后续产品范围调整取代，最终 Robo 仅保留 iPhone 与 Apple Watch 两种来源。各批实际结果以执行记录和顶部当前断点为准。

### 003A · Xcode 命名与工程卫生

范围：

- `Robo Watch App` Scheme 改为 shared `Robo Watch`。
- 确认并移除旧 `sheRuntime` Scheme。
- `Copy Files` 显示名整理为 `Embed Watch Content`。
- 修复测试 Target 中旧 `sheRuntime.app` / `sheRuntime` Host 引用。
- 清理 `xcuserdata`、`.DS_Store` 和 Git 暂存区旧路径记录。

方式：Xcode UI 操作优先由用户手动完成，我负责提供逐项路径、读取结果和编译验证。

验收：iOS/watchOS 编译；iPhone 与 Watch 各启动一次。

估算：0.5–1.5 小时。

### 003B · 仅迁入 Watch UI 与资源

范围：

- 从 ENERGY Watch 复制 `EnergyFaceView`、`FaceParts`、Watch 资源和隐私清单到 Robo 新目录。
- 保留现有 Robo Watch App 入口，只把入口内容切换为迁入的 Watch UI。
- 暂不接入录音和 WatchConnectivity。

验收：watchOS 编译；Apple Watch 真机检查视觉、布局和交互外观。

估算：1–2 小时。

### 003C · 建立小型 Models 与 Contracts

范围：

- 共享 `EnergyTier` 等纯 Swift 模型。
- 定义记录来源：`iPhone`、`appleWatch`、`m5Stack`。
- 定义消息 envelope：`protocolVersion`、`messageId`、`sessionId`、`eventType`、`createdAt`、去重标识。
- 不创建包含所有代码的巨大 Shared 目录。

验收：双 Target 编译；纯模型编码/解码及去重单元测试。

估算：2–4 小时。

### 003D · 迁入 Watch 录音运行时

范围：

- 迁入 `WatchRuntime` 与 `WatchAudioRecorder`。
- 保持 watchOS 录音权限、音频会话和生命周期独立。
- 先在 Watch 本地完成录音状态和临时文件生成，不立即连接 iPhone 时间线。

验收：Apple Watch 真机授权、开始录音、停止录音、不闪退。

估算：2–4 小时。

### 003E · WatchConnectivity 双端协议

范围：

- Watch 发送带完整 envelope 的文件和事件。
- Robo iPhone 新增独立 Connectivity service，接收文件、验证协议版本并去重。
- 保留 ENERGY iPhone Demo 源码作为参考，但不复制其 App 入口和 Demo UI。

验收：真机录音文件 Watch → iPhone；重复消息不产生重复记录；离线传输恢复验证。

估算：4–8 小时。

### 003F · 接入 Robo iPhone 现有业务

范围：

- 将 Apple Watch 录音接入现有音频保存、转写和时间线流程。
- 明确 iPhone、Apple Watch 两种来源。
- Robo 不再包含 M5Stack/CoreBluetooth 实现。
- 不改变现有算法、UI 和业务表现，除非适配来源字段必需。

验收：iPhone 与 Apple Watch 两种来源分别回归。

估算：4–8 小时。

### 003G · 回归、文档与 Git 整理

范围：

- iOS/watchOS Debug 编译及测试。
- iPhone、Apple Watch 真机回归。
- 最终目录树、Target/Scheme、迁移对照表、遗留问题和 commits 拆分。

估算：3–6 小时，主要取决于真机问题。

## 总体估算

迁移启动时估算剩余 17–33 小时；该历史估算现已基本消耗完毕，不再代表当前工作量。

截至 2026-09-03，预计剩余 2–5 小时：

- 文件级旧命名清理及每步验证：1–2 小时。
- 003G 最终回归、目录树/迁移表/遗留问题/commit 拆分和 Git 状态整理：1–3 小时。
- 004 多语言重构与后续架构优化不属于 003 剩余工时，将在稳定迁移基线之后单独估算。

## 协作原则

- 每次只执行一个批次，不跨批混做。
- 涉及 Xcode Scheme、Target、Signing、Capabilities 和 Build Phase 的操作，优先由用户手动点击；我提供精确步骤并在操作后只读验证。
- 源码复制、结构调整、协议实现、测试和迁移文档由我完成。
- 每批自动编译完成后必须由用户做对应真机验证；未确认前不进入下一批。
- she-runtime 与 ENERGY-Watch 原目录保持不动，所有新增和修改只发生在 robo。

## 后续架构议题：多语言重构时机

- 当前 iPhone 主界面主要通过 `copy_zh.json`、`copy_en.json` 和 `CopyProvider/C.t(...)` 取文案，但 Debug 探针、部分服务错误、Audio/HealthKit Probe 及 Apple Watch UI 仍有硬编码中文；系统显示名另由 `InfoPlist.strings` 管理。现状是多套方式并存，不适合在 003F 业务接线中顺带全量替换。
- 003F 新增的用户可见文案继续沿用现有 JSON 机制，避免本批同时改变功能流和本地化基础设施；仅 DEBUG 探针文案可暂时保持硬编码。
- 建议在 003G 完成功能迁移、双端真机回归并形成稳定基线后，单独建立 004 多语言重构批次；它应早于大范围架构优化和正式发布文案冻结。
- 004 再评估统一迁移到 Apple String Catalog（`.xcstrings`）、参数/复数格式、iPhone 与 Watch 的 catalog 边界、缺失键检查，以及 `CopyProvider` 的分阶段退役。`ask_knowledge_zh.json` 属于知识内容，不应与 UI 本地化资源混为一类迁移。

## 004 之后：选择性吸收 she-runtime 更新

- 用户计划在 004 多语言重构完成后，选择 she-runtime 的部分 PR、分支或 commit 功能迁入 Robo；该工作不属于当前 003G，也不在 004 中夹带执行。
- Robo 的 iPhone 源码最初于 2026-09-01 从 she-runtime `pr-15` 工作区复制，基线可追溯到 `cc22378`；该 iOS 内容后来亦包含于 `main@6674de0`。001 当时未正式记录 commit hash，此结论来自后续 reflog 恢复。
- Robo 与 she-runtime 不是可直接延续提交历史的同一工作树，且 Robo 已完成 `iPhoneApp` 路径、Target/Bundle ID、WatchConnectivity、共享契约、双端来源和测试命名迁移，并明确移除 M5Stack/CoreBluetooth。因此不得默认整分支 merge 或无审查 cherry-pick。
- 每次择取前应先记录：上游 PR/分支/commit、涉及文件、与 Robo 当前路径的映射、是否依赖已删除的 M5Stack、是否影响 iPhone/Watch 共享契约、数据模型与多语言资源。
- 推荐以独立小批处理：先读取上游 diff，再手工移植或选择性应用可兼容变更；随后执行 Product Test、iPhone 真机和必要的 Apple Watch 回归，并在迁移日志中记录来源 commit，确保可追溯。

## 003F 执行记录

### 2026-09-03 · 第一小批：Watch 录音进入现有确认流程（待真机验证）

- 用户确认 Watch 录音不自动写入时间线：先进入独立 FIFO 待处理队列，逐条复用现有端侧中文转写和文本确认界面；确认后保存，取消后删除音频。
- 新增 `Connectivity/WatchRecordingInbox.swift`，只负责按 messageId 去重、保持到达顺序及中断后回到队首；它不依赖 SwiftUI、转写服务或 SwiftData。
- `MainTabView` 监听 iPhone Connectivity service 已接收的文件：前台时尝试处理，后台时仅入队，回到 active 后继续。
- `VoiceCaptureViewModel` 为 Watch 导入保留来源、messageId、Watch 创建时间与文件 URL；复用原有检查、端侧转写、编辑确认和保存路径，不自动覆盖正在进行的 iPhone 录音或另一条 Watch 录音。
- Watch 记录使用 envelope messageId 作为 `TimelineRecord.id`，创建时间沿用 envelope createdAt，来源写为 `Apple Watch voice`；iPhone 既有来源 `iPhone voice` 不变。
- Watch 确认界面的“重试”重新转写同一文件；不会错误启动一次 iPhone 麦克风录音。保存或取消后继续处理队列中的下一条。
- App 在 Watch 文件转写期间进入后台时，会取消本次任务并把文件放回队首；不会删除该文件。当前队列仍是进程内队列，App 被强制终止时未确认文件的恢复留待本批真机主流程通过后评估。
- 本批新增正式文案继续写入现有 `copy_zh.json`/`copy_en.json`，未启动 004 多语言重构。
- 新增 FIFO、重复过滤和中断回队测试。Swift 文件语法解析及 JSON 格式检查通过。
- 命令行完整构建仍被本机 Watch Asset Catalog 环境阻塞：`No available simulator runtimes for platform watchsimulator`。独立 iOS type-check 继续受 SwiftData/Predicate 宏插件沙箱限制；未删除资源或功能绕过，等待用户在 Xcode 编译与真机验证。

### 2026-09-03 · 第一小批首轮反馈修复（待复测）

- Product Test 出现 5 个 `Cannot use mutating member on immutable value: '$0' is immutable`。原因是 Swift Testing 的 `#expect` 宏直接包裹了 5 次会改变队列的 `popFirst()` 调用；已改为先取出结果、再做只读断言，未修改队列业务行为。
- 用户明确补充真实使用场景：Apple Watch 可能在骑车等 iPhone App 未打开的情况下录音，待之后与手机连接/打开 Robo 时自动上传并转写。原实现只持久保存 m4a、待处理 envelope 仅驻留内存，无法覆盖 App 被终止后的恢复。
- iPhone 接收每条 Watch m4a 时现同步写入同名 JSON sidecar，保存完整 envelope、时长、能量等级、接收时间和文件路径；Robo 下次启动会扫描有效 sidecar，按 Watch 创建时间恢复 FIFO 队列。保存或取消记录时同时清除 m4a 与 sidecar。
- 队列入队统一按 envelope createdAt 排序，避免 App 启动时批量恢复多条录音的处理顺序被 Debug 探针的显示顺序影响。
- 源码语法解析、两份 copy JSON 格式和 diff whitespace 检查通过；完整 Product Test 仍等待用户在 Xcode 复测。
- “重新说”当前对 Apple Watch 来源定义为重新转写同一个 Watch 文件，因此完成后确认面板会再次出现。用户反馈其无法关闭，但期望动作尚需确认：关闭/丢弃当前录音、重新转写原文件，或改用 iPhone 麦克风重新录制。本轮未擅自改变该业务语义。

### 2026-09-03 · Watch 确认页取消语义确认

- 用户确认 Apple Watch 来源不需要“重新说/重新转写”动作；该按钮改为“取消并删除”。
- 点击后删除当前 Watch m4a 与对应 JSON sidecar，并关闭当前确认页；若队列中还有其他 Watch 录音，继续处理下一条。
- iPhone 本机录音仍显示“重新说”并启动 iPhone 麦克风重录，既有行为不变。
- 按钮文案通过现有 copy JSON 提供；未启动多语言基础设施重构。Swift 语法、JSON 格式与 diff whitespace 检查通过，等待 Xcode/真机复测。

### 2026-09-03 · 003F 第一小批真机结果与 TODO

- 用户确认业务验收项均通过：iPhone 未打开时 Watch 可录音，恢复连接并打开 Robo 后可以按队列转写和确认；“取消并删除”、后续条目继续处理、保存到 Today，以及 iPhone 本机“重新说”行为均正常。
- `Product → Test` 仍未通过。此前 5 个与测试中 mutating `popFirst()` 断言相关的错误在用户复测环境中尚未确认消失，因此本项保留为 TODO，不将测试状态误记为通过。
- TODO：下次处理时获取 Xcode 当前完整错误文本与对应行号，确认 Xcode 是否仍编译旧诊断、是否出现新的测试编译错误，再做最小修复；在获得证据前不改业务实现。
- 当前结论：003F 第一小批的真机功能通过，但自动测试门禁未通过，批次处于“功能完成、测试待修复”状态。

### 2026-09-03 · Product Test 剩余 5 项定位与修复

- 用户提供 Xcode 展开的诊断，确认剩余 5 项均位于 `MessageDeduplicatorTests.rejectsDuplicatesAndEvictsOldestKeyAtCapacity()` 第 69–73 行，并非此前已修复的 Watch inbox `popFirst()` 断言，也不是旧缓存。
- 根因相同：Swift Testing 的 `#expect` 宏展开后将被捕获值视为不可变，不能在表达式内直接调用 mutating `insertIfNew()`。
- 已将 5 次 mutating 调用移出 `#expect`，先保存 Bool 结果，再分别验证首次插入、拒绝重复、容量插入、淘汰及重新插入语义；未修改 `MessageDeduplicator` 实现或任何业务代码。
- TODO 状态保留至用户重新执行 `Product → Test` 并确认结果。

### 2026-09-03 · Product Test TODO 关闭

- 用户重新执行 `Product → Test`，确认修复后测试通过；003F 第一小批的功能与自动测试门禁现均通过。
- 关闭 Product Test TODO，进入独立的测试 Target 命名迁移批次。
- 命名迁移继续遵守不手工编辑 `project.pbxproj`：先由 Xcode 重命名 Target，再验证测试 Host/Scheme，最后分别处理目录和 Bundle ID，每一步后单独测试。

### 2026-09-03 · 测试 Target 第一阶段重命名（待验证）

- 用户通过 Xcode 将 `sheRuntimeTests` Target 改为 `RoboTests`，将 `sheRuntimeUITests` Target 改为 `RoboUITests`。
- 只读验证 Target 列表现为 `Robo`、`RoboTests`、`RoboUITests`、`Robo Watch App`；共享 Scheme 仍仅有 `Robo` 与 `Robo Watch`，未产生多余测试 Scheme。
- `RoboTests` 的 TEST_HOST 仍指向 `Robo.app/.../Robo`，`RoboUITests` 的 TEST_TARGET_NAME 仍为 `Robo`，宿主关系正确。
- 本步有意保留旧 `productName`、测试 Bundle ID 和磁盘目录，等待 Product Test 通过后再分别迁移，避免一次改变多个工程标识。

- 用户完成本阶段 `Product → Test`，单元测试与 UI Tests 均正常，未出现 Test Host、Bundle 或 Executable 错误；测试 Target 第一阶段重命名验证通过。

### 2026-09-03 · 测试 Bundle ID 迁移（待验证）

- 用户通过 Xcode 将 `RoboTests` 的 Debug/Release Bundle ID 改为 `com.arieslx.robo.tests`，将 `RoboUITests` 的 Debug/Release Bundle ID 改为 `com.arieslx.robo.ui-tests`。
- 只读核对确认旧 `com.arieslx.sheRuntimeTests` / `com.arieslx.sheRuntimeUITests` 已不再出现在构建配置中；TEST_HOST 及 TEST_TARGET_NAME 仍正确指向 `Robo`。
- 本步未修改测试磁盘目录、Swift 测试类型或 `project.pbxproj` 中由 Xcode维护的旧历史 productName 字段，等待 Product Test 通过后再处理目录展示名。

- 用户重新执行 `Product → Test` 并确认全部正常；测试 Bundle ID 迁移验证通过。下一步仅处理测试同步目录和文件展示名。

### 2026-09-03 · 测试目录重命名核对

- 用户已通过 Xcode 将测试文件改为 `RoboTests.swift`、`RoboUITests.swift`、`RoboUITestsLaunchTests.swift`，UI Test 目录也已正确改为 `RoboUITests`。
- 单元测试目录实际为 `Robotests`，与 Target `RoboTests` 的大写 `T` 不一致；需先通过 Xcode 两段式大小写重命名修正，再做 Product Test。
- 用户同时发现 iPhone 源码同步目录仍名为 `sheRuntime`。确认这是原仓库目录名残留；测试命名稳定后，将单独迁为 `iPhoneApp`，形成 `iPhoneApp / WatchApp / Shared` 对称结构，不与产品 Target `Robo` 混为同名目录。

- 用户已通过 Xcode 两段式重命名，将 `Robotests` 修正为 `RoboTests`。只读核对确认磁盘目录、Xcode 同步路径和 Target 名均一致；`RoboTests.swift`、`RoboUITests.swift`、`RoboUITestsLaunchTests.swift` 路径正确，测试 Host/Target Application 未改变。等待 Product Test 验收。

- 用户完成测试目录迁移后的 `Product → Test`，确认全部正常；测试 Target、Bundle ID、目录与文件展示名迁移验收通过。下一步进入 iPhone 源码同步目录命名迁移。

### 2026-09-03 · iPhone 源码目录迁移（配置待修复）

- 用户通过 Xcode 将同步目录 `sheRuntime` 改为 `iPhoneApp`；磁盘目录与 Xcode 文件同步路径均已正确更新，源码、Assets、InfoPlist.strings、copy JSON、知识 JSON 和 Diagnostics/Connectivity 子目录均存在。
- Target 列表仍为 `Robo`、`RoboTests`、`RoboUITests`、`Robo Watch App`，目录重命名未创建或删除 Target。
- Xcode 未自动更新 Robo Target 的 `CODE_SIGN_ENTITLEMENTS`：Debug/Release 仍指向已不存在的 `sheRuntime/sheRuntime.entitlements`，实际文件为 `iPhoneApp/sheRuntime.entitlements`。在修复该构建设置前不执行编译，也不修改其他文件名。

- 用户已在 Xcode 将 Robo Target 的 Debug/Release `CODE_SIGN_ENTITLEMENTS` 修正为 `iPhoneApp/sheRuntime.entitlements`。只读核对确认文件存在，工程配置与共享 Scheme 中不再包含 `sheRuntime/` 旧目录路径。等待 iPhone 编译、真机启动和 Product Test 验收。

- 用户已完成 `iPhoneApp` 目录迁移后的全部验收：Robo iPhone 真机安装/启动及原页面正常，`Product → Test` 通过，`Robo Watch` 可编译并在 Watch 启动。iPhone 源码目录迁移批次正式完成。

## 003A 执行记录

### 2026-09-02 · Scheme 与测试宿主整理（进行中）

- 用户已在 Xcode Manage Schemes 中将 `Robo Watch App` 重命名为 `Robo Watch`。
- 已生成 `Robo.xcodeproj/xcshareddata/xcschemes/Robo Watch.xcscheme`。
- `Robo Watch.xcscheme` 的 Target 为 `Robo Watch App`，容器引用为 `container:Robo.xcodeproj`，验证正确。
- `xcodebuild -list` 当前列出 `Robo`、`Robo Watch`、`sheRuntime`。
- 用户重新切换 `Robo` 的 Shared 状态后，已生成 `Robo.xcodeproj/xcshareddata/xcschemes/Robo.xcscheme`。
- `Robo.xcscheme` 中 App、单元测试和 UI 测试的容器引用均为 `container:Robo.xcodeproj`，验证正确。
- 两个目标正式 Scheme `Robo` 与 `Robo Watch` 现均已 shared 并落盘。
- 旧 `sheRuntime.xcscheme` 仍引用 `container:RoboWatchApp.xcodeproj`；按照本批约定暂未删除或手工修改。
- 用户已在 Robo Target 的 Build Phases 中将 `Copy Files` 重命名为 `Embed Watch Content`。
- 只读验证确认 Destination 对应 `dstSubfolderSpec = 16`（Products Directory），Subpath 为 `$(CONTENTS_FOLDER_PATH)/Watch`，嵌入项为 `Robo Watch App.app`。
- 用户已将单元测试 Debug/Release 的 `TEST_HOST` 修正为 `$(BUILT_PRODUCTS_DIR)/Robo.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/Robo`。
- Xcode General 页确认 UI 测试的 `Target Application` 为 `Robo`；构建依赖图也确认 `sheRuntimeUITests` 显式依赖 `Robo`。底层旧 `TEST_TARGET_NAME = sheRuntime` 暂不手工改写 `project.pbxproj`，以 Xcode 展示和实际依赖关系为准。
- 首次 `build-for-testing` 暴露单元测试源码仍使用 `@testable import sheRuntime`。已仅将该导入修正为 `@testable import Robo`，未修改测试逻辑或测试 Target 名称。
- 修正后再次执行 `build-for-testing`，旧模块解析错误已消失，且四个 Target 的依赖图均成功生成。
- 第二次命令行验证受本机环境阻塞：`xcodebuild` 无法连接 `CoreSimulatorService`，随后 iOS SwiftData 宏插件服务返回 malformed response。该问题未通过修改业务代码规避，等待用户从 Xcode 内执行测试构建确认。
- 用户已完成本批修改后的 iPhone 真机功能回归，确认 App 可以安装、启动且现有功能可用。
- 用户随后执行 `Product → Test`；测试运行至 `inBedDoesNotBridgeSeparateSleepSessions()` 时触发运行时断点，因此测试套件暂不标记为通过。
- 已定位为现有 `DailyHealthSummaryLogic.clippedInterval` 的无交集边界缺陷：函数在确认裁剪后的开始时间早于结束时间之前就构造 `DateInterval`。当测试中的两个独立睡眠段互相裁剪时会形成反向区间并触发 Foundation 断言。此问题并非 Watch Target 合并引入，尚未修改算法，等待用户确认处理方式。
- 经用户确认，已修复上述边界缺陷：先计算并校验 `clippedStart`、`clippedEnd`，仅在 `clippedEnd > clippedStart` 时构造 `DateInterval`，否则返回 `nil`。睡眠合并规则、90 分钟会话间隔和测试预期均未改变，等待用户重新执行 `Product → Test`。
- 用户重新执行测试后不再发生 `DateInterval` 运行时断点。当前剩余 4 条 expectation failure：三个睡眠时长测试及 `rapidDateChangesIgnoreLateResultsAndUpdateAllMetricsTogether()` 的睡眠时长断言。
- 四条失败中 Xcode 展示的实际值与预期数值一致（`21600.0`/`21600`、`25200.0`/`25200`、`1560.0`/`1560`）；共同差异是左侧为 `TimeInterval`（`Double`），右侧为整型算式。初步判定为 Swift Testing 断言两侧类型问题，不是业务计算值偏差；尚未修改测试。
- 经用户确认，已将这 4 条断言的期望值显式转换为 `TimeInterval`。测试输入、预期秒数和产品逻辑均未改变，等待用户再次执行 `Product → Test`。
- 用户再次执行 `Product → Test`，确认测试已全部完成并通过。
- 003A 验证状态：iPhone 真机功能回归通过，`Robo` 测试套件通过；此前 Watch 真机安装、启动和 `Robo Watch` 直接运行基线也已通过。
- 收尾只读清点发现：正式 shared Scheme `Robo`、`Robo Watch` 均存在；旧 shared `sheRuntime.xcscheme` 仍存在；Xcode `xcuserdata` 文件仍存在；Git 暂存区仍保留早期 `Apple/sheRuntime` 的新增后删除记录。尚未执行删除或 Git 索引清理。
- 经用户明确确认，已删除失效的 shared `sheRuntime.xcscheme`，未改写 `project.pbxproj`。
- 已清除 Robo 工程内部的 `xcuserdata`；`.gitignore` 已包含 `xcuserdata/` 与 `*.xcuserstate`。
- 已从 Git 暂存索引清除早期、工作区已不存在的 `Apple/sheRuntime` 导入记录；未删除 `Apple/Robo` 内容，也未操作外部 she-runtime 或 ENERGY-Watch 原仓库。
- `xcodebuild -list` 验证成功：当前 Target 为 `Robo`、`sheRuntimeTests`、`sheRuntimeUITests`、`Robo Watch App`，shared Scheme 仅为 `Robo`、`Robo Watch`。
- 003A 工程卫生和测试配置工作已完成，等待 iPhone 与 Apple Watch 各启动一次的最终真机确认。
- 用户已完成最终真机确认：`Robo` Scheme 可在 iPhone 正常启动，`Robo Watch` Scheme 可在 Apple Watch 正常启动并显示当前 Hello World 模板。003A 正式完成。

## 003B 执行记录

### 2026-09-02 · Watch UI 与资源盘点（进行中）

- 本批只从 ENERGY-Watch 读取并复制 Watch UI/资源到 Robo；不修改 ENERGY-Watch 原目录。
- 暂不迁入录音、WatchConnectivity、iPhone Demo 或业务接线。
- 已确认 Watch 视觉文件为 `EnergyFaceView.swift`、`FaceParts.swift` 和 Watch `Assets.xcassets`；另有 Watch `PrivacyInfo.xcprivacy`。
- `EnergyFaceView` 直接持有 `WatchRuntime` 环境对象并调用 `pressBegan()` / `pressEnded()`；`WatchRuntime` 同时耦合 AVFoundation 录音与 WatchConnectivity，因此不能在“不接入录音和通信”的 003B 中原样连带迁入。
- `FaceParts` 依赖 `EnergyTier`，而原 `EnergyTier.swift` 同时包含纯枚举、显示文案、SwiftUI `Color`、调色板及颜色扩展，尚未按共享模型与平台样式拆分。
- 在复制文件前暂停，等待用户确认采用临时展示状态适配，还是调整 003B/003C 的执行边界。
- 用户确认采用参数化展示状态方案。
- 已从 ENERGY-Watch 原样复制 `EnergyFaceView.swift`、`FaceParts.swift`、`EnergyTier.swift`、Watch 图片资源及 `PrivacyInfo.xcprivacy` 到 Robo；原 ENERGY-Watch 未修改。
- Robo 新增 `UI/` 与 `Presentation/` 小目录。`EnergyFaceView` 已改为接收 `tier`、`phase`、`elapsed`、`levels` 和按压回调，不再直接依赖 `WatchRuntime`；新增仅含 UI 状态的 `FacePhase`。
- 保留现有 Robo Watch App 入口；`ContentView` 现展示 `.steady` 待机状态的 `EnergyFaceView`。本批按压回调为空，未接入录音或 WatchConnectivity。
- 静态核对确认 Robo Watch 目录不包含 `WatchRuntime`、AVFoundation 或 WatchConnectivity 引用；隐私清单有效，全部 Asset Catalog JSON 可解析。
- 命令行 watchOS 编译进入资源编译阶段后受环境阻塞：`actool` 报告 `No available simulator runtimes for platform watchsimulator`，同时 `CoreSimulatorService` 不可连接。没有通过修改资源或功能绕过，等待用户在 Xcode 中进行真机编译验证。
- 用户已在配对 Apple Watch 上完成 `Robo Watch` Build/Run，确认能量液面、植物形象、文案和静态待机界面正常；按压无录音响应符合本批预期。
- 用户已再次在 iPhone 真机运行 `Robo`，确认原有功能正常。003B 正式完成。

## 003C 执行记录

### 2026-09-02 · Models 与 Contracts 只读核对（进行中）

- 先检查 Robo 现有记录来源字段、类型命名和测试结构；未确认冲突前不新增共享协议文件。
- 现有 `TimelineRecord.source` 是持久化字符串，当前值为 `iPhone voice` 与 `sheRuntime StopWatch`。003C 若直接替换成新枚举会牵涉 SwiftData 旧数据兼容，因此建议先新增三来源契约与旧值映射，不改现有字段或写入逻辑。
- M5Stack 当前内部 BLE 协议已有 `UInt16 sessionId`；WatchConnectivity envelope 的 `sessionId` 应采用独立的字符串/UUID 表示，不能复用或混同 M5Stack BLE 会话类型。
- iPhone 与 Watch 源码当前分别属于两个 `PBXFileSystemSynchronizedRootGroup`。若建立顶层 `Shared/Models`、`Shared/Contracts` 并让两端编译同一份文件，需要通过 Xcode UI 添加文件夹引用/Target Membership；不会手工修改 `project.pbxproj`。
- Watch 本地 `Presentation/EnergyTier.swift` 同时包含纯模型和 SwiftUI 样式。003C 建议把纯 `EnergyTier` 移到共享 Models，把 title/suggestion/fill/color/palette 等留在 Watch Presentation 扩展中。
- 用户在 VS Code 中将 Watch 源码目录改名为 `WatchApp`，但 Xcode 的文件同步根仍指向旧路径 `Robo Watch App Watch App`，导致工程无法读取该组。
- 已将磁盘目录可逆恢复为旧名，使其重新匹配现有 Xcode 工程引用；未修改源码或 `project.pbxproj`。下一步应从 Xcode 内执行目录重命名，以便工程安全同步路径。
- 用户随后在 Xcode 内将同步源码目录重命名为 `WatchApp`。只读验证确认磁盘路径与 `PBXFileSystemSynchronizedRootGroup.path` 均为 `WatchApp`。
- Target 仍为 `Robo Watch App`，Scheme 仍为 `Robo Watch`；`xcodebuild -list` 成功，目录整理未改变产品配置。等待 Watch 真机启动确认。
- 用户已使用 `Robo Watch` Scheme 在 Apple Watch 真机完成 Build/Run，确认能量待机页面正常。`WatchApp` 目录重命名验证通过。

### 2026-09-02 至 2026-09-03 · 产品范围调整：移除 M5Stack（进行中）

- 用户明确调整产品范围：新的 Robo 不再实现或保留 M5Stack/StopWatch 功能。外部 she-runtime 与 ENERGY-Watch 原仓库仍保持不动。
- 初步盘点确认 M5Stack 代码只存在于 Robo iPhone 侧，Apple Watch 新 UI 未引用 CoreBluetooth 或 StopWatch。
- 候选删除文件：`StopWatchBLEService.swift`、`StopWatchBLEProbeViewModel.swift`、`StopWatchBLEProbeView.swift`、`StopWatchAudioPipelineService.swift`。
- 候选连接点清理：`AppServices` 的 BLE/音频管线、`MainTabView` 的 StopWatch 流监听、`ProfileView`/`DataPrivacyView` 的蓝牙权限与设备卡片、`DataPrivacyModels` 的 StopWatch 状态枚举、`TimelineRecordSource.stopWatch`、调试页 BLE 入口。
- 候选配置与文案清理：中英文 StopWatch/蓝牙文案、`DataAccessPermissionService` 中未使用的 CoreBluetooth import，以及 iPhone Target 的 `NSBluetoothAlwaysUsageDescription` Build Setting。
- 尚未执行删除；先等待用户确认完整范围。删除后将分别进行 iPhone/Watch 编译与真机回归，再恢复 003C 共享契约工作。
- 用户已确认完整删除范围。已删除四个 StopWatch 专用实现文件，并清除 `AppServices`、`MainTabView`、`ProfileView`、`DataPrivacyView`、`DataPrivacyModels`、`TimelineRecordSource` 和调试页中的所有连接点。
- 已移除 `DataAccessPermissionService` 中的 CoreBluetooth import，并从中英文文案删除蓝牙、StopWatch、设备绑定和 StopWatch 音频相关内容；保留 iPhone 录音、麦克风权限与 HealthKit。
- 已清理重新出现但不含源码的旧 `Apple/sheRuntime/RoboWatchApp.xcodeproj` workspace/user-state 残留；当前正式工程仍仅位于 `Apple/Robo`。
- 静态扫描确认 Robo 源码中不再存在 `M5Stack`、`StopWatch`、`CoreBluetooth`、蓝牙文案或 `TimelineRecordSource.stopWatch` 引用；中英文 JSON 均通过解析校验。
- 当前仅剩 iPhone Target Debug/Release 的 `NSBluetoothAlwaysUsageDescription` Build Setting，需要通过 Xcode UI 删除；完成后再进行 iPhone/Watch 编译与真机回归。
- 用户已在 Xcode 中清空该设置；虽然 `All` 视图仍显示系统支持的父级名称，但只读检查确认 `project.pbxproj` 中已不存在 `INFOPLIST_KEY_NSBluetoothAlwaysUsageDescription` 或其他蓝牙权限赋值，配置清理完成。
- 清理后分别执行 iOS 与 watchOS 命令行构建；两者均在嵌入的 Watch Asset Catalog 阶段被本机环境阻塞，错误为 `No available simulator runtimes for platform watchsimulator` / `CoreSimulatorService connection became invalid`。日志中未出现 M5Stack/StopWatch 源码断引用；未通过修改资源绕过。
- 等待用户从 Xcode 分别执行 `Robo` 与 `Robo Watch` 真机 Build/Run，并检查 iPhone 数据与隐私页已不再显示蓝牙或 StopWatch 内容。
- 用户已完成双端真机验证：iPhone 主要功能正常，数据与隐私页仅保留 Apple 健康和麦克风；Apple Watch 能量待机页面正常。M5Stack/StopWatch 移除批次正式完成。
- 后续 003C–003G 的产品范围仅包含 iPhone 与 Apple Watch，不建立 M5Stack 来源、协议或兼容层。

### 2026-09-03 · 最小共享契约实施（进行中）

- 用户确认先完成迁移闭环，再于 004 分批优化架构；003C 只建立当前功能必需的边界。
- 新增 `Shared/Models/EnergyTier.swift` 与 `RecordSource.swift`；`RecordSource` 仅定义 `iPhone`、`appleWatch`。
- 新增 `Shared/Contracts/WatchEventType.swift`、`WatchMessageEnvelope.swift`、`MessageDeduplicator.swift`。
- Envelope 包含 `protocolVersion`、`messageId`、`sessionId`、`eventType`、`createdAt`、`deduplicationKey`；当前协议版本为 1，消息与会话 ID 使用 UUID。
- 去重器为容量受限的纯内存值类型，默认保存最近 256 个 key；持久化去重留待 WatchConnectivity 接入批次。
- 将 Watch 原 `Presentation/EnergyTier.swift` 拆为纯共享模型与 `EnergyTier+Presentation.swift`；颜色、中文文案、液面比例、Palette 和 Color 扩展仍只属于 Watch 表现层，视觉值未修改。
- 静态核对确认 Shared 不依赖 SwiftUI，且 Shared/Watch 中不存在 M5Stack、StopWatch、CoreBluetooth 或蓝牙引用。
- 当前 `Shared` 尚未加入 Xcode 工程及 Target Membership，属于不可编译的中间状态；等待用户通过 Xcode UI 将同一 Shared 文件夹加入 `Robo` 与 `Robo Watch App` 两个 Target。
- 用户已通过 Xcode 以 `Reference files in place` / `Create groups` 添加 `Shared`，并只勾选 `Robo`、`Robo Watch App`。
- 只读工程验证确认 5 个 Shared 文件均为单一文件引用，并各自进入 iPhone 与 Watch Sources Build Phase；未直接加入测试 Target。
- 新增纯模型测试：Envelope JSON 编解码保留全部协议元数据、默认去重 key 使用 messageId、重复 key 拒绝及容量满后淘汰最旧 key。测试通过 iPhone App 模块访问 Shared 类型。
- 5 个 Shared 文件已通过独立 Swift type-check。
- iOS `build-for-testing` 与 watchOS Build 均再次受本机 `CoreSimulatorService`/Watch Asset Catalog 环境阻塞，错误仍为 `No available simulator runtimes for platform watchsimulator`；未出现 Shared 源码编译错误，也未修改资源绕过。
- 等待用户在 Xcode 执行 `Product → Test`，并分别在 iPhone、Apple Watch 真机启动确认。
- 用户已完成验证：`Product → Test` 全部通过，iPhone 原功能正常，Apple Watch 能量待机页面正常。003C 正式完成。

## 003D 执行记录

### 2026-09-03 · Watch 录音运行时只读拆分（进行中）

- 本批只迁入 Apple Watch 本地录音权限、音频会话、计时/音量状态与临时文件生成。
- WatchConnectivity 发送、iPhone 接收和时间线写入继续留到后续批次。
- 已新增 `WatchApp/Recording/WatchAudioRecorder.swift`：使用 AAC、22.05 kHz、单声道中等质量写入 Robo Watch 临时目录，并提供录音电平。
- 已新增 `WatchApp/Recording/WatchRuntime.swift`：保留按住录音、松开保存、60 秒自动停止、计时、波形、触觉反馈及 saved/fallback 状态；完成后仅保存 `latestRecordingURL`，不发送文件。
- `ContentView` 已将运行时状态与回调传给参数化 `EnergyFaceView`；Robo Watch App 入口持有 `WatchRuntime`，并在场景离开 active 时结束当前按压录音。
- 原 Demo 的 WatchConnectivity import、WCSession 激活、phoneConnected、application context 和 transferFile 均未迁入。
- 针对 watchOS 10 最低版本，将已弃用的 `AVAudioSession.requestRecordPermission` 等价替换为 `AVAudioApplication.requestRecordPermission()`。
- Shared 与 Watch 录音核心文件使用 watchOS 10 SDK 独立 type-check 通过；全 UI type-check 仅受本机 Preview 宏插件沙箱错误阻塞。
- Watch Target 尚未配置 `NSMicrophoneUsageDescription`，在添加用途说明前不得进行真机录音测试。
- 用户已通过 Xcode 为 `Robo Watch App` 添加 `Privacy - Microphone Usage Description`，值为“用于在 Apple Watch 上录制语音记录”。只读验证确认 Watch Debug/Release 均包含该键，且未覆盖 iPhone 自身的麦克风说明。
- 003D 进入 Apple Watch 真机授权与本地录音验证阶段。
- 用户已在 Apple Watch 真机完成麦克风授权并成功进入录音、计时、松开保存流程。
- 当前录音位于 Watch App 沙盒临时目录，003D 未提供播放或文件浏览 UI，也未进行 WatchConnectivity 传输，因此音频内容本身尚未试听验证；计划在下一通信批次传到 iPhone 后验证。
- 003D 尚待 iPhone 原功能回归确认后结束。
- 用户已在 iPhone 真机确认 `Robo` 可以正常启动和使用。003D 双端回归完成，批次正式结束。

## 003E 执行记录

### 2026-09-03 · WatchConnectivity 文件传输只读核对（进行中）

- 先分析 ENERGY iPhone Demo 的接收行为、Robo 现有音频文件存储边界以及 Shared envelope 的传输表示。
- 第一小批只建立 Watch → iPhone 文件落地、协议版本校验和去重，不接入转写或时间线。
- ENERGY Demo 在 `WCSessionDelegate.didReceive file` 回调中同步复制系统临时文件，这一时序必须保留；回调返回后原临时 URL 可能失效。
- Robo 现有 `AudioFileStore` 专用于 iPhone Audio Probe 的临时录音，不适合作为 Watch 接收目录；003E 建议使用独立的 Application Support/WatchRecordings 目录。
- Shared envelope 可直接映射到 WCSession file metadata 的 property-list 基础类型：UUID 使用字符串、createdAt 使用时间戳，并附带 duration 与 EnergyTier rawValue；iPhone 先解析并校验协议版本/事件类型，再按 deduplicationKey 去重。
- 本批不接入转写或时间线，若不增加临时验证入口，用户只能从 Xcode 控制台确认接收，无法在 iPhone 试听音频。等待用户确认使用 DEBUG-only 播放探针，或接受控制台加 App Container 手动验证。
- 用户确认增加可随时删除的 DEBUG-only 播放探针，并要求与正式迁移代码解耦。
- Shared `WatchRecordingTransferMetadata` 将 envelope 六个字段与 duration、energyTier 映射为 WCSession file metadata 的 property-list 基础类型；新增 round-trip 测试。
- Watch 新增独立 `Connectivity/WatchConnectivityService.swift`，负责会话激活与 `transferFile`；`WatchRuntime` 仅通过 `WatchRecordingTransferring` 协议调用，不依赖 iPhone 或探针 UI。
- 每次 Watch 录音开始生成独立 sessionId，完成后以 `.audioFile` envelope 排队传输；传输错误不破坏本地录音成功状态，并单独保留 `transferError`。
- iPhone 新增独立 `Connectivity/PhoneWatchConnectivityService.swift`。接收回调中先解析元数据、校验 protocolVersion/eventType，再同步复制到 Application Support/Robo/WatchRecordings；文件以 messageId 命名，并结合文件存在性与内存去重器避免重复。
- iPhone 新增 `Diagnostics/WatchTransferProbeView.swift`，整个文件受 `#if DEBUG` 保护，仅负责显示会话、收到的文件和播放；正式 Connectivity service 不导入 AVFoundation。
- Profile 仅在 Debug 构建显示虫子图标 NavigationLink；Release 不包含探针入口。当前尚未接入转写或 TimelineRecord。
- 当前实现要求录音完成时 WCSession 已激活；会话未激活时仅保留 Watch 本地文件，尚无重试队列。离线/重试恢复将在在线真机传输通过后单独补齐，不冒充已完成。
- 两端 Connectivity service 已补充显式 Combine 依赖。
- Shared 契约、Watch 录音+发送核心、iPhone 接收核心分别使用 Swift、watchOS 10 SDK、iOS SDK 独立 type-check，三组均通过。
- 等待用户执行 `Product → Test`，并通过 Debug 探针完成首次在线 Watch → iPhone 录音传输与播放验证。

### 2026-09-03 · 中断恢复审计

- 只读确认当前仍位于 `feat/refector` 分支；未清理、提交或重排现有工作树。
- 003E 的 Shared 元数据契约、Watch 发送服务、iPhone 接收服务、DEBUG-only 播放探针及契约 round-trip 测试文件均仍存在，Xcode 工程中的 `Shared`、`WatchApp` 与 `sheRuntime` 文件同步引用也仍存在。
- 上次中断没有造成已实现代码丢失。当前完成边界仍是“代码实现及静态 type-check 通过”，不能将 003E 标记为完成，因为 Product Test 和首次 Watch → iPhone 真机传输/播放尚未获得用户确认。
- 本次未继续实现离线重试：应先验证在线主链路，避免将传输、重试与业务接入同时扩大为一个不可定位的测试批次。

### 2026-09-03 · DEBUG 探针编译修复

- 真机构建前发现 `WatchTransferProbeView` 的三个编译错误，均源自探针播放器使用 `ObservableObject`、`@Published` 与 `@StateObject` 时缺少定义模块导入。
- 仅为 DEBUG-only 探针文件补充显式 `import Combine`；未修改 WatchConnectivity、录音、文件保存、业务逻辑或 UI 行为。
- 等待用户重新构建并执行 003E 真机在线传输测试。

### 2026-09-03 · 003E 在线链路验证

- 用户报告本批测试完成；按当前测试清单记录为 Robo Product Test 通过，Watch 录音能够传到 iPhone Debug 探针并播放。
- 在线主链路通过后，下一项先验证 `WCSession.transferFile` 自带的离线排队恢复；在验证失败前不额外实现自定义重试队列。
- 如果用户此次仅完成编译而未完成录音传输或播放，应撤回在线链路完成结论并继续定位，不进入离线测试。

### 2026-09-03 · 003E 离线恢复验证完成

- 用户在双端会话激活后关闭 iPhone，并在 Apple Watch 离线录制两条内容不同的音频；iPhone 恢复后两条录音均由系统队列自动送达 Debug 探针。
- 两条文件均可正常播放，接收数量与录制数量一致，未出现重复记录，Watch 与 iPhone 均未显示错误。
- `WCSession.transferFile` 已满足本阶段在线与离线可靠传输需求，不增加自定义重试队列，避免与系统队列形成双重发送。
- 003E 验收项（在线传输、协议元数据、去重、离线恢复与播放）全部通过，批次正式完成。下一批进入 003F，只处理 Apple Watch 录音接入 Robo iPhone 现有业务流程。
