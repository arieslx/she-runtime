# 002 · Robo 工程命名与空 Watch Target

- 日期：2026-09-01
- 分支：`feat/refector`
- 状态：完成；空 Watch Target 已正确嵌入，并通过模拟器及 iPhone/Apple Watch 真机验证

## 已确认命名

| 项目 | 目标值 |
|---|---|
| 仓库 | `robo` |
| 产品显示名 | `Robo` |
| Xcode 工程 | `Robo.xcodeproj` |
| iPhone Target / Scheme | `Robo` |
| Watch Target | `Robo Watch App` |
| Watch Scheme | `Robo Watch` |
| iPhone Bundle ID | `com.arieslx.robo` |
| Watch Bundle ID | `com.arieslx.robo.watchkitapp` |

用户已于 2026-09-01 明确确认以上命名。Swift 类型暂不批量重命名。

## 本批目标

1. 通过 Xcode 安全配置把 robo 副本的工程身份改为 Robo。
2. 创建一个配套 iPhone App 的空 watchOS App Target。
3. 建立 `Robo Watch` shared scheme。
4. 验证 iOS、watchOS 以及 iPhone 嵌入 Watch App 的编译。
5. 本批不迁移 ENERGY Watch 业务源码和资源。

## 当前执行状态

- 第 001 批 iPhone 真机验证已经通过。
- 工程已机械整理为 `Apple/Robo/Robo.xcodeproj`。
- iPhone Target 已通过 Xcode 重命名为 `Robo`。
- Watch Target 已通过 Xcode 创建并重命名为 `Robo Watch App`。
- `xcodebuild -list` 当前可见 `Robo`、`Robo Watch App`、`sheRuntime` 三个 Scheme；尝试把 `Robo Watch App` 改为 `Robo Watch`，但名称未保存，因此仍待后续处理。旧 `sheRuntime` Scheme 尚待确认清理时机。
- Watch 模板源码仍位于临时目录 `Robo Watch App Watch App/`，尚未迁移 ENERGY 代码。
- 尚未手工修改 `project.pbxproj`。
- iPhone Target 已通过 Xcode 添加对 `Robo Watch App` 的 Target Dependency。
- iPhone Target 已通过 Xcode 创建 Copy Files Build Phase，Destination 为 Products Directory，Subpath 为 `$(CONTENTS_FOLDER_PATH)/Watch`，并加入 `Robo Watch App.app`。

## 校验发现的未完成配置

通过 `xcodebuild -list`、`-showBuildSettings` 及只读工程配置检查发现：

| 配置 | 当前值 | 目标值 |
|---|---|---|
| iPhone Target | `Robo` | `Robo` |
| Watch Target | `Robo Watch App` | `Robo Watch App` |
| iPhone Scheme | `Robo` 与旧 `sheRuntime` 并存 | `Robo` |
| Watch Scheme | `Robo Watch App` | `Robo Watch` |
| iPhone Bundle ID | `com.arieslx.robo` | `com.arieslx.robo` |
| Watch Bundle ID | `com.arieslx.robo.watchkitapp` | `com.arieslx.robo.watchkitapp` |
| Watch companion Bundle ID | `com.arieslx.robo` | `com.arieslx.robo` |
| Watch Deployment Target | watchOS 10.0 | watchOS 10.0 |
| iPhone → Watch Target dependency | 已建立 | 必须存在 |
| Embed Watch Content | 已建立，阶段暂名 `Copy Files` | 必须存在；后续仅整理显示名 |

核心配置已完成，可以进入真机验证；Scheme 和构建阶段的显示名称仍需后续整理。

## 环境阻塞

Computer Use 可以读取 Xcode 主窗口并通过键盘完成部分操作，但对部分 Target 配置区的点击会导致 UI 控制管道中断。2026-09-01 首次尝试写入 Watch companion Bundle ID 和 Product Bundle ID 时，Xcode 中的值未改变。

用户开启辅助功能权限后，2026-09-02 已通过 Xcode 界面完成 Bundle ID、companion ID、Deployment Target、Target Dependency 和 Watch 嵌入阶段配置。没有把命令行临时 build setting 当作持久配置，也没有手工编辑工程文件。

## 自动编译结果

### Watch

执行 `Robo Watch App` Scheme 的 watchOS Simulator Debug 编译。结果：`BUILD SUCCEEDED`。由于当前自动生成 Scheme 的构建关系，该次构建同时包含 Robo iPhone Target；Watch Target 本身已成功编译为 watchOS 10.0。

### iPhone 与嵌入 Watch App

执行 `Robo` Scheme 的 iOS Simulator Debug 编译。结果：`BUILD SUCCEEDED`。

构建日志确认：

- Target dependency graph 包含 `Robo → Robo Watch App`。
- Watch App 被复制到 `Robo.app/Watch/Robo Watch App.app`。
- `ValidateEmbeddedBinary` 成功完成。
- 保留了原 iPhone 工程已有的 AppIcon 尺寸和未分配 SVG 警告，本批没有通过删除或改资源绕过。

## 真机验证清单

1. 在 Xcode 选择 `Robo` Scheme 和配对 iPhone 真机，执行 Run。
2. 确认 Robo iPhone App 能安装、启动，原有页面和主要功能仍正常。
3. 在 iPhone 的 Watch App 中确认配套的 `Robo Watch App` 可安装到已配对 Apple Watch。
4. 在 Apple Watch 上启动 `Robo Watch App`，确认空模板界面正常显示且不闪退。
5. 单独选择 `Robo Watch App` Scheme 和 Apple Watch 真机运行，确认安装和启动正常。
6. 本批尚未迁移 WatchConnectivity 或 ENERGY Watch 业务代码，因此不测试跨端消息同步。

## 真机验证结果

用户于 2026-09-02 逐项确认：

- `Robo` Scheme 可运行到配对 iPhone。
- iPhone App 可安装、启动，原有页面及主要功能正常。
- iPhone 的 Watch App 可看到并安装配套 Robo Watch App。
- Apple Watch 上的模板 App 可启动且不闪退。
- `Robo Watch App` Scheme 可直接运行到 Apple Watch 真机，界面显示 `Hello, world!`。

因此第 002 批基线验收通过。后续命名整理和 ENERGY Watch 功能迁移必须进入新批次，不在本批继续扩大范围。

## 已知未完成项

- Watch Scheme 仍名为 `Robo Watch App`，尚未保存为目标名称 `Robo Watch`。
- 旧 `sheRuntime` Scheme 仍存在，删除前需要单独确认。
- Embed 阶段功能正确，但显示名仍为 `Copy Files`，尚未整理为 `Embed Watch Content`。
- shared scheme 磁盘文件仍需整理；当前仅明确存在旧 `sheRuntime.xcscheme`。
- Watch 模板目录仍为 `Robo Watch App Watch App/`。
- 测试 Target 内仍有 `sheRuntime.app` / `sheRuntime` 的旧 Host 引用，需在后续独立批次验证和修正。

## 人工接力位置

当前工程路径为 `Apple/Robo/Robo.xcodeproj`。自动编译门槛已通过，下一步由用户执行上述 iPhone 与 Apple Watch 真机验证；验证通过后再继续 Scheme/目录整理和 ENERGY Watch 业务迁移。

## 原始仓库保护

本批所有操作范围仍限定为 `/Users/ari/Desktop/robo`。`/Users/ari/Desktop/she-runtime` 与 `/Users/ari/Desktop/ENERGY-Watch` 保持不动。
